{
  "name": "itinerary-map",
  "version": "0.1.0",
  "description": "Trip planning in Obsidian: paste Google Maps links, write days as headings, see the plan on a map beside the note.",
  "type": "module",
  "license": "MIT",
  "author": "Kevin Lin",
  "repository": { "type": "git", "url": "git+https://github.com/kevinsslin/obsidian-itinerary-map.git" },
  "keywords": ["obsidian", "obsidian-plugin", "travel", "itinerary", "map", "leaflet"],
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "npm run typecheck && node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "smoke": "node scripts/load-smoke.mjs",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build && npm run smoke",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@codemirror/state": "6.5.0",
    "@codemirror/view": "^6.34.0",
    "@eslint/js": "^9.15.0",
    "@types/leaflet": "^1.9.22",
    "@types/node": "^22.9.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.15.0",
    "obsidian": "^1.13.1",
    "tslib": "^2.8.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.16.0",
    "vitest": "^3.2.0"
  }
}
